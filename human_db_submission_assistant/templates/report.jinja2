## 研究利用申請審査結果レポート

### 申請 ID

{{ application_id }}

### 研究概要

{{ abstract }}

ICD10 コード: {{ abstract_icd10 }}

### 論文情報

| 論文 タイトル | ICD-10 コード | 取得元 URL |
| ------------ | ------------ | --------- |
{% for paper in papers %}| {{ paper.title }} | {{ paper.icd10 }} | {{ paper.url }} |
{% endfor %}

### データセット情報

| データセット ID | ICD-10 コード | 利用目的との類似 | 論文情報との類似 | 解析手法類似度 |
| -------------- | ------------ | -------------- | -------------- | ------------ |
{% for dataset in datasets -%}| {{ dataset.id }} | {{ dataset.icd10 }} | {{ "◯" if dataset.purpose_similarity else "✗"
}} | {{ "◯" if dataset.paper_similarity else "✗" }} | {{ dataset.analysis_method_similarity }} |
{% endfor %}

### データセット詳細評価

{% for dataset in datasets %}
#### データセット ID: {{ dataset.id }}

{{ dataset.description }}

**目的との類似度評価:**
{{ "◯" if dataset.purpose_similarity else "✗" }}
{{ dataset.purpose_similarity_details }}

**論文情報との類似度評価:**
{{ "◯" if dataset.paper_similarity else "✗" }}
{{ dataset.paper_similarity_details }}

**解析手法との類似度評価:**
{{ dataset.analysis_method_similarity }}
{{ dataset.analysis_method_similarity_reason }}

{% endfor %}