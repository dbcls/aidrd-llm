services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./results:/app/results
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
